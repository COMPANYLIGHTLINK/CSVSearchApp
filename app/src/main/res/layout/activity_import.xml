<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:padding="28dp"
        android:paddingTop="48dp">

        <!-- Icon -->
        <ImageView
            android:layout_width="88dp"
            android:layout_height="88dp"
            android:src="@drawable/ic_csv"
            android:contentDescription="CSV icon"
            app:tint="@color/primary"
            android:alpha="0.9"
            android:layout_marginBottom="20dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Import CSV Data"
            android:textSize="24sp"
            android:textStyle="bold"
            android:textColor="@color/text_primary"
            android:layout_marginBottom="16dp" />

        <!-- Hint card -->
        <com.google.android.material.card.MaterialCardView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="24dp"
            app:cardCornerRadius="12dp"
            app:cardElevation="2dp"
            app:cardBackgroundColor="#EEF2FF">

            <TextView
                android:id="@+id/tvImportHint"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="16dp"
                android:textSize="13sp"
                android:textColor="@color/text_primary"
                android:lineSpacingMultiplier="1.5" />

        </com.google.android.material.card.MaterialCardView>

        <!-- Import from Android/data/ (shown when file detected) -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnImportExternal"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:text="Import data.csv"
            android:textSize="15sp"
            app:cornerRadius="28dp"
            android:layout_marginBottom="10dp"
            android:visibility="gone" />

        <!-- Pick file from anywhere -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnPickFile"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:text="📂  Pick CSV File"
            android:textSize="15sp"
            style="@style/Widget.Material3.Button.OutlinedButton"
            app:cornerRadius="28dp"
            android:layout_marginBottom="10dp" />

        <!-- Refresh — recheck Android/data/ folder -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnRefresh"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:text="🔄  Refresh (I just copied the file)"
            android:textSize="13sp"
            style="@style/Widget.Material3.Button.TextButton"
            android:layout_marginBottom="6dp" />

        <!-- Assets button (hidden unless data.csv in assets) -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnImportAssets"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:text="Use Bundled data.csv (assets)"
            android:textSize="13sp"
            style="@style/Widget.Material3.Button.TextButton"
            android:layout_marginBottom="6dp"
            android:visibility="gone" />

        <!-- Help -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnHelp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="❓  How to import?"
            style="@style/Widget.Material3.Button.TextButton"
            android:layout_marginBottom="24dp" />

        <!-- Progress -->
        <ProgressBar
            android:id="@+id/progressBar"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="8dp"
            android:max="100"
            android:visibility="gone"
            android:layout_marginBottom="8dp" />

        <TextView
            android:id="@+id/tvProgress"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="13sp"
            android:textColor="@color/primary"
            android:visibility="gone" />

        <!-- Error -->
        <com.google.android.material.card.MaterialCardView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            app:cardCornerRadius="8dp"
            app:cardBackgroundColor="#FFEEEE"
            app:cardElevation="0dp">

            <TextView
                android:id="@+id/tvError"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="12dp"
                android:textColor="#CC0000"
                android:textSize="13sp"
                android:lineSpacingMultiplier="1.4"
                android:visibility="gone" />

        </com.google.android.material.card.MaterialCardView>

    </LinearLayout>

</ScrollView>
